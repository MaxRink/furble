// Host CameraList shim. src/FurbleConsole.cpp includes the header but prints
// the saved list from the UI task, so no CameraList symbol is referenced.
#ifndef FURBLE_HOST_CONSOLE_CAMERA_LIST_H
#define FURBLE_HOST_CONSOLE_CAMERA_LIST_H

#endif
