// Host TinyGPSPlus shim. src/FurbleConsole.cpp includes the parser header but
// reads every field through the GPS status snapshot, so nothing of the parser
// itself is needed here.
#ifndef FURBLE_HOST_CONSOLE_TINY_GPS_PLUS_H
#define FURBLE_HOST_CONSOLE_TINY_GPS_PLUS_H

#endif
