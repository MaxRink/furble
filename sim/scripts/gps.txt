# Open Settings > GPS > GPS Data and leave the page visible long enough for
# the real FurbleGPS task and LVGL timer to consume the fake UART stream.
wait 300
key down
wait 200
key left
wait 250
key down
wait 200
key down
wait 200
key left
wait 250
# GPS starts enabled, so the page shows the full receiver configuration list.
# Six focus steps skip baud, Update rate, Sentences, Constellation, and Power
# to reach GPS Data.
key down
wait 200
key down
wait 200
key down
wait 200
key down
wait 200
key down
wait 200
key down
wait 200
key left
wait 1200
capture gps
# Print the $PCAS commands the firmware sent to the fake receiver.
uart-dump
exit
