# Docs walkthrough capture, dark theme, M5StickS3 135x240. Run through the
# capture harness (sim/scripts/docs-capture.sh) with FURBLE_SIM_THEME=Dark and
# the optional-capability env vars set. Filenames are prefixed dark- so they sit
# beside the default-theme set in docs/img.
seed fauxny true
seed gps true
seed saved_camera true
seed imu true
seed imu_wake 1
seed imu_trigger true
wait 500

home
wait 300
capture dark-main
print ui.page

action nav connect
wait 400
capture dark-connect-list
print ui.page

home
wait 300
action nav scan
wait 700
capture dark-scan-list
print ui.page

home
wait 300
action nav settings
wait 350
capture dark-settings
print ui.page

action nav features
wait 350
capture dark-features
print ui.page

action nav sensors
wait 350
capture dark-settings-sensors
print ui.page

action nav gestures
wait 350
capture dark-settings-gestures
print ui.page

action nav infrared
wait 350
capture dark-infrared
print ui.page

action nav gps
wait 350
capture dark-settings-gps
print ui.page

action nav gps_sats
wait 2800
capture dark-gps-satellites
print ui.page

action nav bluetooth
wait 350
capture dark-bluetooth
print ui.page

action nav feedback
wait 350
capture dark-feedback
print ui.page

action nav storage
wait 500
capture dark-storage
print ui.page

home
wait 250
action connect
wait 1800
capture dark-connected
print ui.page

action blind
wait 500
capture dark-remote
print ui.page

exit
