# Two distinct advertisements arrive from the virtual radio and are matched by
# the production CameraList::match in one UI drain. Both rows must be
# materialized and the UI/PM1 heartbeat must continue.
seed fauxny false
seed ble_peers fuji-pair
seed scan_timeout 2
seed watchdog true
seed scan_start_probe true
wait 300

home
wait 200
action nav scan
wait 500
assert ui.page scan
assert-eventually 2000 camera.count 2
# The scan timeout expires on the virtual radio's own timer, which is what
# delivers the production scan end callback.
wait 2000
assert-eventually 2000 scan.end_callbacks 1
assert scan.start_probe_blocked 0
assert platform.watchdog armed
exit
