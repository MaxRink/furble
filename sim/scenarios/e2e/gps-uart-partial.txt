# ACK bytes arrive one at a time. The production receive buffer must retain
# the partial frame until the checksum and full payload arrive.
seed gps true
seed gps_rate 1
seed gps_uart_mode partial
wait 5000
assert-eventually 2000 gps.config.0.state ACKED
assert gps.config.0.attempts 1
exit
