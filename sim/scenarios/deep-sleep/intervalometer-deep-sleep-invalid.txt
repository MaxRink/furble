# Invalid resume metadata is rejected before any reconnect or shutter command.
seed fauxny true
seed autoconnect true
seed ivl_sleep true
seed timed_wake true
seed resume_fixture invalid
wait 5000
assert platform.timed_wake yes
assert platform.wake_marker yes
assert camera.shutter_presses 0
assert ui.interval_state idle
exit
