# A failed timed power-off setup returns to the intervalometer. The second
# shot completes while awake instead of losing the run or exiting the host.
seed fauxny true
seed autoconnect true
seed ivl_sleep true
seed ivl_sleep_thr 10
seed timed_poweroff_fail true
seed interval_count 2
seed interval_delay 20
seed interval_shutter 30
seed interval_wait 0
wait 5000
action intervalometer
wait 30000
assert platform.timed_wake yes
assert platform.wake_marker no
assert camera.shutter_presses 2
assert ui.interval_state finished
exit
