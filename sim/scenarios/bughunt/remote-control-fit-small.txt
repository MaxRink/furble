# Physical-button Remote shutter fit at the smallest text size. The shared
# matrices run touch by default, so this lane preserves the shipped layout's
# strict fit contract on every modeled physical-button board.
seed no_touch true
seed text_size 0
seed fauxny true
seed saved_camera true
wait 400
assert ui.nav_layout buttons
assert setting.text_size 0

action connect
wait 3200
assert ui.connected yes
action page shutter
wait 300
assert ui.page shutter
assert ui.control_mode shutter
assert ui.overflow no
assert ui.label_overlaps 0
assert ui.cut_labels 0
assert ui.indicator_clearance clear

home
wait 250
action blind
wait 300
assert ui.page shutter
assert ui.control_mode shutter
assert ui.overflow no
assert ui.label_overlaps 0
assert ui.cut_labels 0
assert ui.indicator_clearance clear
exit
