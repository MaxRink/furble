# The modeled Core touch panel keeps all three full-size Remote controls in a
# single row, so its shutter page remains fit-required.
seed no_touch false
seed fauxny true
seed saved_camera true
wait 400

action connect
wait 3200
assert ui.connected yes
action page shutter
wait 300
assert ui.nav_layout touch
assert ui.page shutter
assert ui.control_mode shutter
assert ui.overflow no
assert ui.label_overlaps 0
assert ui.cut_labels 0
exit
