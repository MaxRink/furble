# Supplemental touch-layout fit for the five-row Home state at Large text.
# The seeds exactly match text-size-overflow-large; the touch guard makes the
# physical no-touch runner skip this stricter viewport-specific assertion.
seed text_size 2
seed fauxny true
seed gps true
wait 300
assert ui.nav_layout touch
assert setting.text_size 2
assert ui.page main
assert ui.overflow no
exit
