# Core touch Remote shutter fit at the smallest text size. Core touch keeps
# all three full-size controls on one row and therefore remains fit-required.
seed no_touch false
seed text_size 0
seed fauxny true
seed saved_camera true
wait 400
assert ui.nav_layout touch
assert setting.text_size 0

action connect
wait 3200
assert ui.connected yes
action page shutter
wait 300
assert ui.page shutter
assert ui.control_mode shutter
assert ui.overflow no
assert ui.label_overlaps 0
assert ui.cut_labels 0
exit
