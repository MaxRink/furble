# boards/

Board definitions are PlatformIO metadata. Keep flash size, PSRAM capability,
MCU and upload settings aligned with the board-specific sdkconfig. Do not use
a generic board definition when its flash or PSRAM metadata is different.
